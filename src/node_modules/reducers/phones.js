import {FETCH_PHONE_LOADING,  FETCH_PHONE_SUCCESS ,FETCH_PHONE_FAILURE} from 'actions'
import * as R from "ramda"
const initialState = {}
let phones = (state = initialState , action)=> {
    switch (action.type) {
        case FETCH_PHONE_LOADING : {
return state
        }
        case FETCH_PHONE_SUCCESS : {
        const newValues = R.indexBy(R.prop('id'),action.payload)
            return R.merge(state,newValues)
        }
        case FETCH_PHONE_FAILURE : {

        }

        default :
            return state
    }
}

export default phones